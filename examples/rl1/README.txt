The booklet.py example in this directory reads in a document,
converts all the pages to Form XOBjects, and then uses a
reportlab canvas to print the pages two at a time in a format
suitable for a booklet.
